# 本机（macOS）安装依赖时不要执行 install scripts。
#
# 原因：vite / rollup 的可选依赖 fsevents 会触发 node-gyp rebuild，它要从 nodejs.org
# 下载编译头文件（本机网络下会长时间挂住，导致 npm install 看起来"卡死"）。
# fsevents 只是 macOS 文件监听的原生加速，缺失时 vite 会退化为轮询，功能不受影响。
# 本项目是纯前端，没有需要编译的原生模块，因此整体跳过 install scripts 是安全的。
#
# CI（ubuntu）本来就不会编译 fsevents，这个设置只对本地安装生效。
ignore-scripts=true
